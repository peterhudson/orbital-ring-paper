# 2026-04-29

- Reviewer response on the orbital-ring paper was strongly positive on overall architecture and sign logic. The new crux is now framed as dynamic consistency, actuator topology, and whether the control-allocation story can be made concrete enough to count as more than a sketch.
- I revised the paper accordingly: Section 7.7 now uses a speed-offset-area coupling argument rather than a bare `beta L` estimate, Section 7.6 now states the topology requirement for local power exchange, Section 3.2 now includes a representative lane-level force-density case, Section 6 now includes an explicit four-lane sign table, and Sections 8.2/8.4 now use the `85 GJ/m` scale to state total ring energy and example fault-domain energy.
- I also softened the straight-lane stability claim to "does not provide passive self-centering by momentum redirection alone," introduced more technical language like `prestressed membrane guide shell`, added global figure constraints, and trimmed repeated defensive caveats so the draft reads more like a finished architecture paper.
- The final lingering PDF math artifact in Section 7.7 was a rendered beta/inverse-length formula block. Rewriting it as plain prose eliminated the issue; for this markdown-to-PDF pipeline, plain-language numerical statements are the most reliable fallback when MathJax/Chromium keeps misrendering a formula.
- The reviewer-driven revision was delivered as commit `5c05c33` (`Tighten orbital ring control and closure draft`), after the last PDF cleanup commit `0f58fb6` replaced the final broken beta formula and produced a clean review PDF.
- A later PDF cleanup replaced the broken inline definition of `L_eff` in Section 7.7 with safer prose and converted prose scientific notation from caret form like `10^N` to true superscript characters such as `10⁻⁷` and `10¹⁸`, which are more reliable in this render pipeline. That pass was committed as `07b9a3a`.
- The next major reviewer pass pushed the orbital-ring paper beyond architecture-only concerns and toward closure. I revised the draft to move the primary numerical reference case from 80 km to 500 km, explicitly treat 80 km as only a lower-altitude stress-test, strengthen the prestress discussion by showing that `Gamma = 10` is only a lower-bound shell-prestress case, add a tolerance budget for four-lane imbalance, add a shell/load-path caveat for turning sector tug into ring bending, and append four new screening appendices on dynamic stability, slug discreteness, power/thermal losses, and fault-domain sizing. That revision was committed as `af8ffc3` (`Add closure appendices to orbital ring draft`).
