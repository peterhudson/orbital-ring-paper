# 2026-05-01

- Hudson wants a stricter figure workflow for the orbital-ring paper: do not insert figures into the manuscript until he has explicitly signed off on them, to avoid document pollution and merge/rollback pain.
- For new figures, Hudson prefers a text-first process: first draft detailed text descriptions/specs, then generate SVG only after approval. He also prefers splitting compound figures into separate assets when practical (for example Figure 1a and 1b rather than one split-panel SVG).
- Hudson does not want borders, embedded titles, or caption-style explanatory text inside SVG figures, only diagram-native geometry and labels.
- In figure labels, variable names should use real typographic subscripts rather than underscore notation when they correspond to symbols in the text.
- The orbital-ring paper structure was revised so the old Section 1.3 on reference-frame/orbital-dynamics bookkeeping was moved out of the introduction into Appendix E, because Hudson felt it was too algebra-heavy and out of place in Section 1. The main result there is that U_g << u, so it is safe as appendix material. The former Section 1.4 was renumbered to Section 1.3, a later Section 8.1 cross-reference was updated, and the PDF was regenerated. Commit: 09091ae (`Move inertial-frame bookkeeping to appendix`).
- Figure 1a is in active iteration. Current accepted direction is a side view of Earth with the orbital ring as a slightly tilted equatorial hoop, solid where visible in front and dashed only where actually occluded by Earth, with a dotted Earth rotation axis and minimal labels. The latest revision aimed to match Hudson's markup by making the upper occluded segment dashed, the lower front segment solid, moving the highlighted detail sector to the lower-right front side, and placing the `section shown in detail in Fig. 1b` label in the lower-right without overlapping Earth. Latest commit: cf605b5 (`Adjust figure 1a visibility and detail callout`).
- A first draft of Figure 1b was generated as separate assets at `figures/figure-1b-orbital-ring-local-helical-lanes.{svg,png,pdf}` and committed as `63355d6` (`Add draft figure 1b local helical lanes diagram`). Quick QC: it looks technically intact, but it is visually dense and currently includes many embedded callouts/explanatory labels, so it likely does not yet satisfy Hudson's preferred minimalist text-first figure style. It has not been inserted into the manuscript.
# 2026-05-01

- Hudson wants a stricter figure workflow for the orbital-ring paper: do not insert figures into the manuscript until he has explicitly signed off on them, to avoid document pollution and merge/rollback pain.
- For new figures, Hudson prefers a text-first process: first draft detailed text descriptions/specs, then generate SVG only after approval. He also prefers splitting compound figures into separate assets when practical (for example Figure 1a and 1b rather than one split-panel SVG).
- Hudson does not want borders, embedded titles, or caption-style explanatory text inside SVG figures, only diagram-native geometry and labels.
- In figure labels, variable names should use real typographic subscripts rather than underscore notation when they correspond to symbols in the text.
- The orbital-ring paper structure was revised so the old Section 1.3 on reference-frame/orbital-dynamics bookkeeping was moved out of the introduction into Appendix E, because Hudson felt it was too algebra-heavy and out of place in Section 1. The main result there is that U_g << u, so it is safe as appendix material. The former Section 1.4 was renumbered to Section 1.3, a later Section 8.1 cross-reference was updated, and the PDF was regenerated. Commit: 09091ae (`Move inertial-frame bookkeeping to appendix`).
- Figure 1a is in active iteration. Current accepted direction is a side view of Earth with the orbital ring as a slightly tilted equatorial hoop, solid where visible in front and dashed only where actually occluded by Earth, with a dotted Earth rotation axis and minimal labels. The latest revision aimed to match Hudson's markup by making the upper occluded segment dashed, the lower front segment solid, moving the highlighted detail sector to the lower-right front side, and placing the `section shown in detail in Fig. 1b` label in the lower-right without overlapping Earth. Latest commit: cf605b5 (`Adjust figure 1a visibility and detail callout`).
- A first draft of Figure 1b was generated as separate assets at `figures/figure-1b-orbital-ring-local-helical-lanes.{svg,png,pdf}` and committed as `63355d6` (`Add draft figure 1b local helical lanes diagram`). Quick QC: it looks technically intact, but it is visually dense and currently includes many embedded callouts/explanatory labels, so it likely does not yet satisfy Hudson's preferred minimalist text-first figure style. It has not been inserted into the manuscript.
- Hudson approved the Figure 1b concept before SVG work. The intended composition is a close-up of a gently curved tube section of the torus, showing four helical lanes on the guide-shell surface with at least one full wrap each: two right-handed and two left-handed, each with distinct colour, with one RH and one LH carrying +v and the other RH and LH carrying -v. The figure should also show the centerline with s-hat, tube-radius direction with r-hat, circumferential direction with theta-hat, the helix angle alpha, a note that alpha is exaggerated for clarity, and a single representative decomposition into u_tangential and u_azimuthal. This figure is still awaiting Hudson's review/sign-off.
# 2026-05-01

- Hudson wants a stricter figure workflow for the orbital-ring paper: do not insert figures into the manuscript until he has explicitly signed off on them, to avoid document pollution and merge/rollback pain.
- For new figures, Hudson prefers a text-first process: first draft detailed text descriptions/specs, then generate SVG only after approval. He also prefers splitting compound figures into separate assets when practical (for example Figure 1a and 1b rather than one split-panel SVG).
- Hudson does not want borders, embedded titles, or caption-style explanatory text inside SVG figures, only diagram-native geometry and labels.
- In figure labels, variable names should use real typographic subscripts rather than underscore notation when they correspond to symbols in the text.
- The orbital-ring paper structure was revised so the old Section 1.3 on reference-frame/orbital-dynamics bookkeeping was moved out of the introduction into Appendix E, because Hudson felt it was too algebra-heavy and out of place in Section 1. The main result there is that U_g << u, so it is safe as appendix material. The former Section 1.4 was renumbered to Section 1.3, a later Section 8.1 cross-reference was updated, and the PDF was regenerated. Commit: 09091ae (`Move inertial-frame bookkeeping to appendix`).
- Figure 1a is in active iteration. Current accepted direction is a side view of Earth with the orbital ring as a slightly tilted equatorial hoop, solid where visible in front and dashed only where actually occluded by Earth, with a dotted Earth rotation axis and minimal labels. The latest revision aimed to match Hudson's markup by making the upper occluded segment dashed, the lower front segment solid, moving the highlighted detail sector to the lower-right front side, and placing the `section shown in detail in Fig. 1b` label in the lower-right without overlapping Earth. Latest commit: cf605b5 (`Adjust figure 1a visibility and detail callout`).
- A first draft of Figure 1b was generated as separate assets at `figures/figure-1b-orbital-ring-local-helical-lanes.{svg,png,pdf}` and committed as `63355d6` (`Add draft figure 1b local helical lanes diagram`). Quick QC: it looks technically intact, but it is visually dense and currently includes many embedded callouts/explanatory labels, so it likely does not yet satisfy Hudson's preferred minimalist text-first figure style. It has not been inserted into the manuscript.
# 2026-05-01

- Hudson wants a stricter figure workflow for the orbital-ring paper: do not insert figures into the manuscript until he has explicitly signed off on them, to avoid document pollution and merge/rollback pain.
- For new figures, Hudson prefers a text-first process: first draft detailed text descriptions/specs, then generate SVG only after approval. He also prefers splitting compound figures into separate assets when practical (for example Figure 1a and 1b rather than one split-panel SVG).
- Hudson does not want borders, embedded titles, or caption-style explanatory text inside SVG figures, only diagram-native geometry and labels.
- In figure labels, variable names should use real typographic subscripts rather than underscore notation when they correspond to symbols in the text.
- The orbital-ring paper structure was revised so the old Section 1.3 on reference-frame/orbital-dynamics bookkeeping was moved out of the introduction into Appendix E, because Hudson felt it was too algebra-heavy and out of place in Section 1. The main result there is that U_g << u, so it is safe as appendix material. The former Section 1.4 was renumbered to Section 1.3, a later Section 8.1 cross-reference was updated, and the PDF was regenerated. Commit: 09091ae (`Move inertial-frame bookkeeping to appendix`).
- Figure 1a is in active iteration. Current accepted direction is a side view of Earth with the orbital ring as a slightly tilted equatorial hoop, solid where visible in front and dashed only where actually occluded by Earth, with a dotted Earth rotation axis and minimal labels. The latest revision aimed to match Hudson's markup by making the upper occluded segment dashed, the lower front segment solid, moving the highlighted detail sector to the lower-right front side, and placing the `section shown in detail in Fig. 1b` label in the lower-right without overlapping Earth. Latest commit: cf605b5 (`Adjust figure 1a visibility and detail callout`).
- A first draft of Figure 1b was generated as separate assets at `figures/figure-1b-orbital-ring-local-helical-lanes.{svg,png,pdf}` and committed as `63355d6` (`Add draft figure 1b local helical lanes diagram`). Quick QC: it looks technically intact, but it is visually dense and currently includes many embedded callouts/explanatory labels, so it likely does not yet satisfy Hudson's preferred minimalist text-first figure style. It has not been inserted into the manuscript.
- Hudson approved the Figure 1b concept before SVG work. The intended composition is a close-up of a gently curved tube section of the torus, showing four helical lanes on the guide-shell surface with at least one full wrap each: two right-handed and two left-handed, each with distinct colour, with one RH and one LH carrying +v and the other RH and LH carrying -v. The figure should also show the centerline with s-hat, tube-radius direction with r-hat, circumferential direction with theta-hat, the helix angle alpha, a note that alpha is exaggerated for clarity, and a single representative decomposition into u_tangential and u_azimuthal. This figure is still awaiting Hudson's review/sign-off.
- Figure 1b was then revised in several iterative passes without manuscript insertion: first a cleaner labeling-layout patch with boxed colour-coded lane legend entries, external basis vectors, renamed shell callout (`prestressed membrane guide shell`), and removal of the explicit u-component labels (commit 7647ddd, `Refine figure 1b labeling layout`); then a more substantial redesign to a straight local guide tube with grouped RH/LH lane families and front-face alpha callout (commit 3f56865, `Redesign figure 1b as straight helical guide tube`); then a reorientation to an end-on straight-tube view so the open tube cross-section is visible (commit 48e00f4, `Reorient figure 1b to end-on straight tube view`).
- Latest Hudson feedback on Figure 1b after the end-on straight-tube revision: make the tube run at closer to a 30 degree angle rather than about 45 degrees, move the `prestressed membrane guide shell` label upward into a clearer top area, start the helical lanes on the viewer-near side of the front cut face so they stay solid longer instead of immediately going dotted, re-check and fix occlusion so solid versus dotted segments are correct, ensure the direction arrows sit on solid visible portions of the helices, and move the local coordinate labels `s`, `r`, and `theta` farther out along the extended center line so they do not overlap the tube. Figure 1b is still not signed off.
- Hudson then said Figure 1a and 1b do not need further adjustment for now and explicitly asked to insert them into the manuscript so he could inspect their appearance in the final paper PDF. I inserted both figures into the introduction, rebuilt `docs/active-support-orbital-ring-using-momentum-inflated-slug-streams.pdf`, and committed the preview insertion as `6088793` (`Insert Figures 1a and 1b into orbital ring draft`).
- The first rebuilt preview used the wrong old four-lane Figure 1b asset. Hudson then shared the corrected single-lane Figure 1b as a Google Drive SVG (`131rbm6K862jl3LsvqgXnw0kq6jZgJnfv`), which I downloaded to `figures/figure-1b-orbital-ring-local-helical-lanes-single-lane.svg`, validated, swapped into the manuscript, rebuilt the PDF, and verified as the single-lane render.
# 2026-05-01

- Hudson wants a stricter figure workflow for the orbital-ring paper: do not insert figures into the manuscript until he has explicitly signed off on them, to avoid document pollution and merge/rollback pain.
- For new figures, Hudson prefers a text-first process: first draft detailed text descriptions/specs, then generate SVG only after approval. He also prefers splitting compound figures into separate assets when practical (for example Figure 1a and 1b rather than one split-panel SVG).
- Hudson does not want borders, embedded titles, or caption-style explanatory text inside SVG figures, only diagram-native geometry and labels.
- In figure labels, variable names should use real typographic subscripts rather than underscore notation when they correspond to symbols in the text.
- The orbital-ring paper structure was revised so the old Section 1.3 on reference-frame/orbital-dynamics bookkeeping was moved out of the introduction into Appendix E, because Hudson felt it was too algebra-heavy and out of place in Section 1. The main result there is that U_g << u, so it is safe as appendix material. The former Section 1.4 was renumbered to Section 1.3, a later Section 8.1 cross-reference was updated, and the PDF was regenerated. Commit: 09091ae (`Move inertial-frame bookkeeping to appendix`).
- Figure 1a is in active iteration. Current accepted direction is a side view of Earth with the orbital ring as a slightly tilted equatorial hoop, solid where visible in front and dashed only where actually occluded by Earth, with a dotted Earth rotation axis and minimal labels. The latest revision aimed to match Hudson's markup by making the upper occluded segment dashed, the lower front segment solid, moving the highlighted detail sector to the lower-right front side, and placing the `section shown in detail in Fig. 1b` label in the lower-right without overlapping Earth. Latest commit: cf605b5 (`Adjust figure 1a visibility and detail callout`).
- A first draft of Figure 1b was generated as separate assets at `figures/figure-1b-orbital-ring-local-helical-lanes.{svg,png,pdf}` and committed as `63355d6` (`Add draft figure 1b local helical lanes diagram`). Quick QC: it looks technically intact, but it is visually dense and currently includes many embedded callouts/explanatory labels, so it likely does not yet satisfy Hudson's preferred minimalist text-first figure style. It has not been inserted into the manuscript.
# 2026-05-01

- Hudson wants a stricter figure workflow for the orbital-ring paper: do not insert figures into the manuscript until he has explicitly signed off on them, to avoid document pollution and merge/rollback pain.
- For new figures, Hudson prefers a text-first process: first draft detailed text descriptions/specs, then generate SVG only after approval. He also prefers splitting compound figures into separate assets when practical (for example Figure 1a and 1b rather than one split-panel SVG).
- Hudson does not want borders, embedded titles, or caption-style explanatory text inside SVG figures, only diagram-native geometry and labels.
- In figure labels, variable names should use real typographic subscripts rather than underscore notation when they correspond to symbols in the text.
- The orbital-ring paper structure was revised so the old Section 1.3 on reference-frame/orbital-dynamics bookkeeping was moved out of the introduction into Appendix E, because Hudson felt it was too algebra-heavy and out of place in Section 1. The main result there is that U_g << u, so it is safe as appendix material. The former Section 1.4 was renumbered to Section 1.3, a later Section 8.1 cross-reference was updated, and the PDF was regenerated. Commit: 09091ae (`Move inertial-frame bookkeeping to appendix`).
- Figure 1a is in active iteration. Current accepted direction is a side view of Earth with the orbital ring as a slightly tilted equatorial hoop, solid where visible in front and dashed only where actually occluded by Earth, with a dotted Earth rotation axis and minimal labels. The latest revision aimed to match Hudson's markup by making the upper occluded segment dashed, the lower front segment solid, moving the highlighted detail sector to the lower-right front side, and placing the `section shown in detail in Fig. 1b` label in the lower-right without overlapping Earth. Latest commit: cf605b5 (`Adjust figure 1a visibility and detail callout`).
- A first draft of Figure 1b was generated as separate assets at `figures/figure-1b-orbital-ring-local-helical-lanes.{svg,png,pdf}` and committed as `63355d6` (`Add draft figure 1b local helical lanes diagram`). Quick QC: it looks technically intact, but it is visually dense and currently includes many embedded callouts/explanatory labels, so it likely does not yet satisfy Hudson's preferred minimalist text-first figure style. It has not been inserted into the manuscript.
- Hudson approved the Figure 1b concept before SVG work. The intended composition is a close-up of a gently curved tube section of the torus, showing four helical lanes on the guide-shell surface with at least one full wrap each: two right-handed and two left-handed, each with distinct colour, with one RH and one LH carrying +v and the other RH and LH carrying -v. The figure should also show the centerline with s-hat, tube-radius direction with r-hat, circumferential direction with theta-hat, the helix angle alpha, a note that alpha is exaggerated for clarity, and a single representative decomposition into u_tangential and u_azimuthal. This figure is still awaiting Hudson's review/sign-off.
# 2026-05-01

- Hudson wants a stricter figure workflow for the orbital-ring paper: do not insert figures into the manuscript until he has explicitly signed off on them, to avoid document pollution and merge/rollback pain.
- For new figures, Hudson prefers a text-first process: first draft detailed text descriptions/specs, then generate SVG only after approval. He also prefers splitting compound figures into separate assets when practical (for example Figure 1a and 1b rather than one split-panel SVG).
- Hudson does not want borders, embedded titles, or caption-style explanatory text inside SVG figures, only diagram-native geometry and labels.
- In figure labels, variable names should use real typographic subscripts rather than underscore notation when they correspond to symbols in the text.
- The orbital-ring paper structure was revised so the old Section 1.3 on reference-frame/orbital-dynamics bookkeeping was moved out of the introduction into Appendix E, because Hudson felt it was too algebra-heavy and out of place in Section 1. The main result there is that U_g << u, so it is safe as appendix material. The former Section 1.4 was renumbered to Section 1.3, a later Section 8.1 cross-reference was updated, and the PDF was regenerated. Commit: 09091ae (`Move inertial-frame bookkeeping to appendix`).
- Figure 1a is in active iteration. Current accepted direction is a side view of Earth with the orbital ring as a slightly tilted equatorial hoop, solid where visible in front and dashed only where actually occluded by Earth, with a dotted Earth rotation axis and minimal labels. The latest revision aimed to match Hudson's markup by making the upper occluded segment dashed, the lower front segment solid, moving the highlighted detail sector to the lower-right front side, and placing the `section shown in detail in Fig. 1b` label in the lower-right without overlapping Earth. Latest commit: cf605b5 (`Adjust figure 1a visibility and detail callout`).
- A first draft of Figure 1b was generated as separate assets at `figures/figure-1b-orbital-ring-local-helical-lanes.{svg,png,pdf}` and committed as `63355d6` (`Add draft figure 1b local helical lanes diagram`). Quick QC: it looks technically intact, but it is visually dense and currently includes many embedded callouts/explanatory labels, so it likely does not yet satisfy Hudson's preferred minimalist text-first figure style. It has not been inserted into the manuscript.
# 2026-05-01

- Hudson wants a stricter figure workflow for the orbital-ring paper: do not insert figures into the manuscript until he has explicitly signed off on them, to avoid document pollution and merge/rollback pain.
- For new figures, Hudson prefers a text-first process: first draft detailed text descriptions/specs, then generate SVG only after approval. He also prefers splitting compound figures into separate assets when practical (for example Figure 1a and 1b rather than one split-panel SVG).
- Hudson does not want borders, embedded titles, or caption-style explanatory text inside SVG figures, only diagram-native geometry and labels.
- In figure labels, variable names should use real typographic subscripts rather than underscore notation when they correspond to symbols in the text.
- The orbital-ring paper structure was revised so the old Section 1.3 on reference-frame/orbital-dynamics bookkeeping was moved out of the introduction into Appendix E, because Hudson felt it was too algebra-heavy and out of place in Section 1. The main result there is that U_g << u, so it is safe as appendix material. The former Section 1.4 was renumbered to Section 1.3, a later Section 8.1 cross-reference was updated, and the PDF was regenerated. Commit: 09091ae (`Move inertial-frame bookkeeping to appendix`).
- Figure 1a is in active iteration. Current accepted direction is a side view of Earth with the orbital ring as a slightly tilted equatorial hoop, solid where visible in front and dashed only where actually occluded by Earth, with a dotted Earth rotation axis and minimal labels. The latest revision aimed to match Hudson's markup by making the upper occluded segment dashed, the lower front segment solid, moving the highlighted detail sector to the lower-right front side, and placing the `section shown in detail in Fig. 1b` label in the lower-right without overlapping Earth. Latest commit: cf605b5 (`Adjust figure 1a visibility and detail callout`).
- A first draft of Figure 1b was generated as separate assets at `figures/figure-1b-orbital-ring-local-helical-lanes.{svg,png,pdf}` and committed as `63355d6` (`Add draft figure 1b local helical lanes diagram`). Quick QC: it looks technically intact, but it is visually dense and currently includes many embedded callouts/explanatory labels, so it likely does not yet satisfy Hudson's preferred minimalist text-first figure style. It has not been inserted into the manuscript.
- Hudson approved the Figure 1b concept before SVG work. The intended composition is a close-up of a gently curved tube section of the torus, showing four helical lanes on the guide-shell surface with at least one full wrap each: two right-handed and two left-handed, each with distinct colour, with one RH and one LH carrying +v and the other RH and LH carrying -v. The figure should also show the centerline with s-hat, tube-radius direction with r-hat, circumferential direction with theta-hat, the helix angle alpha, a note that alpha is exaggerated for clarity, and a single representative decomposition into u_tangential and u_azimuthal. This figure is still awaiting Hudson's review/sign-off.
- Figure 1b was then revised in several iterative passes without manuscript insertion: first a cleaner labeling-layout patch with boxed colour-coded lane legend entries, external basis vectors, renamed shell callout (`prestressed membrane guide shell`), and removal of the explicit u-component labels (commit 7647ddd, `Refine figure 1b labeling layout`); then a more substantial redesign to a straight local guide tube with grouped RH/LH lane families and front-face alpha callout (commit 3f56865, `Redesign figure 1b as straight helical guide tube`); then a reorientation to an end-on straight-tube view so the open tube cross-section is visible (commit 48e00f4, `Reorient figure 1b to end-on straight tube view`).
- Latest Hudson feedback on Figure 1b after the end-on straight-tube revision: make the tube run at closer to a 30 degree angle rather than about 45 degrees, move the `prestressed membrane guide shell` label upward into a clearer top area, start the helical lanes on the viewer-near side of the front cut face so they stay solid longer instead of immediately going dotted, re-check and fix occlusion so solid versus dotted segments are correct, ensure the direction arrows sit on solid visible portions of the helices, and move the local coordinate labels `s`, `r`, and `theta` farther out along the extended center line so they do not overlap the tube. Figure 1b is still not signed off.
- Hudson then said Figure 1a and 1b do not need further adjustment for now and explicitly asked to insert them into the manuscript so he could inspect their appearance in the final paper PDF. I inserted both figures into the introduction, rebuilt `docs/active-support-orbital-ring-using-momentum-inflated-slug-streams.pdf`, and committed the preview insertion as `6088793` (`Insert Figures 1a and 1b into orbital ring draft`).
- The first rebuilt preview used the wrong old four-lane Figure 1b asset. Hudson then shared the corrected single-lane Figure 1b as a Google Drive SVG (`131rbm6K862jl3LsvqgXnw0kq6jZgJnfv`), which I downloaded to `figures/figure-1b-orbital-ring-local-helical-lanes-single-lane.svg`, validated, swapped into the manuscript, rebuilt the PDF, and verified as the single-lane render.
- The webchat file-upload path appears unreliable for SVG figure handoff. Hudson worked around it by sharing the corrected Figure 1b via Google Drive, and I used that exact single-lane SVG to produce the corrected orbital-ring paper preview. Commit: `5b4ed63`.
- After reviewing the corrected preview, Hudson flagged that the paper text still referred to plural "lanes" when describing Figure 1b. I updated the introduction sentence, Figure 1b alt text, and Figure 1b caption to singular "lane" wording and rebuilt the PDF.
